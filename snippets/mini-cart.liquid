{% if icon == false %}
    {{ 'Cart ' | escape }}{{ '(' | escape }}{{ cart.item_count }}{{ ')' | escape }}
{% else %}
    {% render 'icon--cart' %}{{ '(' | escape }}{{ cart.item_count }}{{ ')' | escape }}
{% endif %}